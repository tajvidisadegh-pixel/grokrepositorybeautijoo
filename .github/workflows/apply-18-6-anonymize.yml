name: apply-18-6-anonymize
on:
  workflow_dispatch:
  push:
    paths:
      - '.github/workflows/apply-18-6-anonymize.yml'
jobs:
  apply:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      - name: Apply anonymize patch
        run: |
          set -euo pipefail
          echo 'SEE_ARTIFACT_FILE'
