name: restore-cache-18-3
on:
  workflow_dispatch:
  push:
    paths:
      - '.github/workflows/restore-cache-18-3.yml'
permissions:
  contents: write
jobs:
  restore:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Restore files
        run: |
          python3 << 'PY'
          import zlib, base64, pathlib
          print('loading...')
          PY
